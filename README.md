# WordGuess

**WordGuess** - консольная игра на Java, аналог Wordle, с цветным выводом в терминал.  
Игроку нужно угадать случайное пятибуквенное слово на русском языке, используя подсказки после каждой попытки.

Цветной вывод реализован с помощью библиотеки **Jansi (AnsiConsole)**.

---

## Описание проекта

После запуска игры случайным образом выбирается слово из словаря пятибуквенных русских слов.  
Игрок вводит свои варианты, а игра подсвечивает каждую букву:
- 🟩 зелёный - буква стоит **на правильном месте**;
- 🟨 жёлтый - буква **есть в слове**, но в другой позиции;
- 🟥 красный - буквы **нет в слове**.

Также в игре отображается виртуальная клавиатура, показывающая состояние каждой буквы алфавита.

---

## Архитектура проекта

Проект разделён на логические слои:

### `Main`
Точка входа приложения:
- инициализация `AnsiConsole`;
- запуск игрового цикла;
- чтение пользовательского ввода через `BufferedReader`;
- обработка исключений;
- завершение игры при победе.

### `Model`
- **WordGuessGame** - основная логика игры:
    - выбор случайного слова;
    - проверка введённого слова;
    - формирование результата каждой попытки;
    - обновление состояния "клавиатуры".
- **GameData** - объект передачи данных между логикой и представлением.
- **DictionaryReader** - загрузка словаря слов из файла.

### `View`
- **WordGuessViewCmd** - полностью отвечает за вывод в консоль:
    - очистка экрана;
    - вывод попыток игрока;
    - цветная подсветка букв;
    - отображение виртуальной клавиатуры;
    - вывод ошибок и сообщений о победе.

### `Exceptions`
Кастомные исключения для обработки ошибок ввода:
- `InvalidWordException`;
- `LengthArrayException`;
- `OutOfAlphabetException`;
- `WordException`.

---

## Логика игры

- Слово выбирается случайно из словаря.
- Каждая попытка сравнивается с загаданным словом:
    - формируется массив результатов (`int[]`), где  
      `0` - буквы нет,  
      `1` - есть, но не на месте,  
      `2` - на правильной позиции.
- Для корректной обработки повторяющихся букв используется временная модификация массива символов загаданного слова.
- Состояние букв сохраняется в `Map<Character, Integer>`.

---

## Используемые технологии

- **Java Core**
- **BufferedReader / InputStreamReader**
- **Collections (List, Map, LinkedHashMap)**
- **Random**
- **Работа с файлами (FileReader)**
- **Maven**
- **Jansi (AnsiConsole)**
- **Кастомные исключения**

---

## Запуск проекта

1. Склонировать репозиторий:
```bash
git clone https://github.com/IJackDaniel/WordGuess.git
```
2. Убедиться, что файл словаря находится по пути:
```bash
Data/russianWords.txt
```
3. Запустить Main.

---

## Что было изучено в проекте
- Разделение логики, представления и данных;
- Работа с пользовательским вводом без `Scanner`;
- Проектирование CLI-приложений;
- Кастомные исключения;
- Работа с внешними библиотеками;
- Цветной вывод в терминале;
- Структурирование проекта и упаковка в Maven-проект.

---

## Возможные улучшения
- Добавление ограничения по количеству попыток;
- Сохранение статистики игр;
- Юнит-тесты;
- Поддержка других языков;
- GUI (JavaFX).

































